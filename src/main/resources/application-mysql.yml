# src/main/resources/application-mysql.yml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/educompound?createDatabaseIfNotExist=true
    username: root
    password: Ishola123?
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: mysql
